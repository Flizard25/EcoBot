import telebot

# Замени '' на токен твоего бота
# Этот токен ты получаешь от BotFather, чтобы бот мог работать
bot = telebot.TeleBot("")

@bot.message_handler(commands=['Plastic'])
def send_welcome(message):
    bot.reply_to(message, "пакет — 20 лет; 
                           кофейный стаканчик — 30 лет; 
                           трубочка для напитков — 200 лет; 
                           пластиковая бутылка — 450 лет; 
                           одноразовый подгузник — 500 лет; 
                           зубная щётка — 500 лет. 
                           Многие виды пластика не разлагаются вовсе.")


@bot.message_handler(commands=['Metal'])
def send_welcome(message):
    bot.reply_to(message, "Алюминий имеет период разложения до 500 лет.
                           Железо разлагается быстрее алюминия — примерно за 90–100 лет.
                           Сталь может разлагаться в течение нескольких сотен лет, в зависимости от условий окружающей среды.
                           Также мелкие металлические материалы, которые обычно используются для банок, бутылок или аэрозолей, разлагаются примерно до 30 лет.")


@bot.message_handler(commands=['Glass'])
def send_welcome(message):
    bot.reply_to(message, "Срок разложения стекла в природе может превышать 1000 лет. При этом специалисты полагают, что стекло до конца не разлагается.")


@bot.message_handler(commands=['Aluminum'])
def send_welcome(message):
    bot.reply_to(message, "Период разложения алюминия составляет до 500 лет. ")
